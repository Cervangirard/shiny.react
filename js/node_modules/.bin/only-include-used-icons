#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@codegouvfr/react-dsfr/bin/only-include-used-icons.js" "$@"
  ret=$?
else 
  node  "$basedir/../@codegouvfr/react-dsfr/bin/only-include-used-icons.js" "$@"
  ret=$?
fi
exit $ret
