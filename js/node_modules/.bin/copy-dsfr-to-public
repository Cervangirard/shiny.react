#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@codegouvfr/react-dsfr/bin/copy-dsfr-to-public.js" "$@"
  ret=$?
else 
  node  "$basedir/../@codegouvfr/react-dsfr/bin/copy-dsfr-to-public.js" "$@"
  ret=$?
fi
exit $ret
